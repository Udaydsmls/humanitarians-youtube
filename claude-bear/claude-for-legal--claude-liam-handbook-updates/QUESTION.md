# QUESTION — claude-for-legal--claude-liam-handbook-updates

**Source note (redo mode, read before anything else):** `SUBJECT.json` points
`source_sheet` at
`/Users/nik/Documents/books/anthropics/claude-for-legal/youtube/claude-liam-handbook-updates/beat_sheet.json`.
That sheet exists and is fully "built" (7 beats, all marked VIDEO/filled,
dated 2026-07-25) but its narration text carries **literal, never-filled
template placeholders** (`>`) at every point where the actual skill-specific
fact should be — `"the skill is handbook-updates. >."`, `"Claude's job: >."`,
`"The SKILL.md is the spec — >."`, `"I want to >."` The `source_skill` field
it names,
`/Users/bear/Documents/CoWork/bear-textbooks/books/anthropics/claude-for-legal/employment-legal/skills/handbook-updates/SKILL.md`,
does not exist anywhere on this machine (searched the whole `books/` tree —
no `handbook-updates` SKILL.md, no `employment-legal` folder). So there are
no real facts to carry over: the source is a teardown-format shell that was
never actually authored. (Same situation, same disposition, as this
factory's `claude-for-legal--claude-liam-ai-inventory` redo — see that
reel's QUESTION.md for the identical pattern.)

**The call (logged, not asked):** rather than block on a missing human
answer, this build reconstructs the evident subject from the title
("HANDBOOK-UPDATES") and the family (`claude-for-legal`, an employment-law
skill set) into a generic, defensible account of what keeping an employee
handbook "updated" actually means and why the naive approach (review it once
a year) misses the point — described generically per the fresh-script
Phase 1 rule ("when in doubt, describe behavior generically") rather than
inventing specific tool names, UI, or product claims. No fact here is
Claude-specific or unverifiable; it is the general shape of a
handbook-maintenance practice (a law changes → the affected section is
flagged → new language is drafted → the change is logged with what changed,
why, and when) as used in employment-law compliance work broadly.

**Question this reel actually answers:** An employee handbook was written
once and mostly forgotten. What actually triggers the need to update it —
a date on the calendar, or something else?

**Who asked, where:** nobody — reconstructed per the note above.
**Name usable:** n/a.
