# QUESTION.md

**Question:** Claude picked up a new "skill" called build-guide — did it actually
learn something new, or is that not what's going on?

**Who asked / where:** Redo-mode reel. Source question carried over from the
locked source script (`claude-liam-build-guide`, a Teardown skill-explainer
under `anthropics/claude-for-legal/`). Not a live viewer submission.

**Name usable:** N/A (no submitter).

**Source-fidelity note:** the source's beat_sheet.json never received its
skill-specific "Claude's job: ___" fill — the literal placeholder `>` is
still present in four places (B00, B03, BVDT, BHTF), and the SKILL.md it
was meant to describe (`/Users/bear/Documents/CoWork/bear-textbooks/books/
anthropics/claude-for-legal/legal-clinic/skills/build-guide/SKILL.md`) does
not exist on this machine. See BUILD-LOG.md for how this redo handled the
gap: the source's true, generic argument (what a skill is, how it executes,
why it's a specification with a limit) is preserved and expanded to meet
hai-simple's six-move spine; the specific legal-domain procedure build-guide
performs is never invented — "build-guide" is used only as the named
example of a skill-shaped folder, per its literal name.
