# QUESTION.md

**Question:** When you add interactive UI to an MCP server, does the tool
itself hand back the widget's HTML — or does something else entirely, a
separately-registered resource, do that job?

**Who asked / where:** Redo-mode reel. Source question carried over from the
locked source script (`claude-liam-build-mcp-app`), a fully-built Teardown
reel under
`anthropics/claude-plugins-official/youtube/claude-liam-build-mcp-app/` that
walks the Build MCP App Claude plugin-dev Skill. Not a live viewer
submission.

**Name usable:** N/A (no submitter).
