# CARRY-OUT.md

**Carry-out line (BCRY, read before any other narration — CARRY-OUT LAW):**

> Same checklist in, the same flagged sections out, every time. Deciding what the new
> handbook actually says is still yours.

**Test:** repeated cold, out of context, next week — still true, still specific to this
skill (names the actual behavior: a repeatable comparison against a jurisdiction
checklist, scoped to whatever the SKILL.md tells it to check), not a generic "AI can
handle legal paperwork" line. It compresses the distinction that matters (flagging what
changed vs. who writes the final language), not the topic (expansion or employment law
in general).

**Wrong guess it defeats:** that "Claude can update our handbook for the new location"
means Claude hands back a finished, ready-to-publish document. The reel's B03 states the
skill's actual constraint (compare the existing policy against the new jurisdiction's
requirements, flag every section that needs a change) and stops there without judging
whether that's a good or bad design; the carry-out is what a viewer should keep: the
skill's reliability is scoped to the spec it was given, and the actual rewrite — the
language that goes into the handbook, any judgment call outside the checklist — is still
counsel's job.

**Source of this line:** compressed from the source reel's `BVDT` verdict-recap
narration ("expansion-update makes Claude execute one task reliably. The SKILL.md is the
spec... Same input, same output, every run. Know the limit: only what the file says.") —
reused as this video's single global carry-out. The redo's only change is which beat
carries it (a REMOTION quote card, one sentence, alone on screen) instead of a
verdict-artifact card with a "Verdict" label; a verdict belongs to Teardown, not Plain,
and this one sentence already does the job without the framing.
