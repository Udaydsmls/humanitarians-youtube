# QUESTION — claude-for-legal--claude-liam-comments

**Source note (redo mode, read before anything else):** `SUBJECT.json`
points `source_sheet` at
`/Users/nik/Documents/books/anthropics/claude-for-legal/youtube/claude-liam-comments/beat_sheet.json`.
That sheet exists and is fully "built" (7 beats, all marked VIDEO/filled,
dated 2026-08-03, Teardown register). Its `source_skill` field,
`/Users/bear/Documents/CoWork/bear-textbooks/books/anthropics/claude-for-legal/regulatory-legal/skills/comments/SKILL.md`,
does not exist on this machine (searched the whole `books/` tree — no
`regulatory-legal` folder anywhere under `claude-for-legal`; only a
`youtube/` subfolder exists there). Same class of missing-source-file gap
already logged for sibling redos in this family
(`claude-for-legal--claude-liam-amendment-history`,
`claude-for-legal--claude-liam-ai-inventory`).

**Unlike those siblings, this source is not a placeholder shell.** Its
narration text already carries real, specific facts about what the
`comments` skill does, not unfilled `>` template markers:

> "Review open NPRM comment periods, log decisions, track deadlines. Use
> when an NPRM has a comment window open and you need to surface
> deadlines, decide whether to file, or record a filing / not-filing /
> waived decision (`--decide CMT-ID`)."

NPRM = Notice of Proposed Rulemaking, the standard US federal-rulemaking
document that opens a public comment window (commonly 30/60/90 days)
before a proposed rule can be finalized — a generic, verifiable fact about
the regulatory process, not a Claude-specific claim. So this redo lifts
the source's actual facts directly (skill name, its three jobs, the
file/not-file/waived decision states, the `--decide CMT-ID` flag) rather
than reconstructing a generic account from just the title, as the
placeholder-shell siblings required.

**Question this reel actually answers:** A newcomer assumes Claude already
knows, on its own, which regulatory comment periods are open and when they
close. It doesn't. What does a Claude skill called `comments` actually do,
and how does tracking a filing deadline actually work?

**Who asked, where:** nobody — redo of an existing Teardown-register reel,
no external asker to attribute.
**Name usable:** n/a.
