# QUESTION.md

**Question:** What does an "exam forecast" Claude skill actually promise — does it know what's on your exam?

**Who asked / where:** Redo-mode reel. Source title and slug carried over from the
locked source sheet (`claude-liam-exam-forecast`,
`anthropics/claude-for-legal/youtube/claude-liam-exam-forecast/beat_sheet.json`),
a `simple`-family, Teardown-register reel about a law-student-facing
`exam-forecast` SKILL.md. Not a live viewer submission.

**Note on source completeness:** the source sheet's `narration_text` and
`props` fields contain unresolved `>` placeholder characters where the
skill-specific content belongs (what exam-forecast's "design tell"
actually is, what its verdict lines actually say), and the `SKILL.md` its
`metadata.source_skill` points at does not exist on this machine (path is
`/Users/bear/...`, a different machine's home directory) or anywhere else
in this repository. The general SKILL.md anatomy the source states —
folder, `SKILL.md` lists steps, Claude executes them in order — is kept
verbatim; the topic-specific facts the source never filled in were
reconstructed generically from the name "exam-forecast" and this family's
sibling law-student skills (`bar-prep-questions`, `flashcards`,
`irac-practice`): a skill that reads a course's syllabus and past exams
and ranks likely topics by how often they've previously been tested. This
is a judgment call, logged here per the honesty rule — see BUILD-LOG.md.

**Name usable:** N/A (no submitter).
