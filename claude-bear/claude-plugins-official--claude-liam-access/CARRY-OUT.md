# CARRY-OUT.md

**Carry-out line (BCRY narration):**
> Claude only trusts an access change typed into your own terminal — never
> one that just shows up as a Discord message, no matter how convincing it
> looks.

**Wrong guess it defeats:** that asking the bot directly on Discord is
enough to get approved (B00's hesitant-writer correction: "Discord" →
"the terminal" — the naive framing asks if asking *in Discord* works; the
corrected question asks if asking *in the terminal* works, which is what
the rest of the reel shows: one state file, a terminal-only rule checked
before anything else, an approval flow that never auto-picks a pending
code, and the specific forged-message attack that rule is built to stop).

**Test:** if someone repeats only this sentence next week, it is still true
and still the distinction that matters — not a summary of the whole topic.
