# CARRY-OUT.md

**Carry-out line (BCRY, read before any other narration — CARRY-OUT LAW):**

> Closing a matter archives it out of the active list, every time — it never deletes
> the record. What gets captured is only what the file tells it to.

**Test:** repeated cold, out of context, next week — still true, still specific to this
skill (names the actual behavior: archive, not delete; capture scoped to what the
SKILL.md specifies), not a generic "AI can manage case files" line. It compresses the
distinction that matters (archive vs. delete, and scope-bound capture), not the topic
(matter management in general).

**Wrong guess it defeats:** that "closing" a matter means Claude deletes or erases it —
the natural reading of "close" for anyone used to closing a ticket, an account, or a
tab. The reel's B03 states the skill's actual constraint (capture outcome, final
exposure, and lessons, then archive out of the active portfolio *without deleting the
record*) and stops there without judging whether that's a good or bad design; the
carry-out is what a viewer should keep: closing archives, it does not erase, and what
gets captured is bounded by the file's own spec.

**Source of this line:** compressed from the source reel's `BVDT` verdict-recap
narration ("matter-close makes Claude execute one task reliably... Same input, same
output, every run. Know the limit: only what the file says.") plus the source's own
task statement ("archive it out of the active portfolio without deleting the record") —
reused as this video's single global carry-out. The redo's only change is which beat
carries it (a REMOTION quote card, one sentence pair, alone on screen) instead of a
verdict-artifact card with a "Verdict" label; a verdict belongs to Teardown, not Plain,
and this sentence already does the job without the framing.
