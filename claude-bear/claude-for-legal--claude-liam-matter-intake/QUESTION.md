# QUESTION.md

**Question:** Claude picked up a skill called matter-intake — does that mean
Claude itself now decides which new matters the firm takes on?

**Who asked / where:** Redo-mode reel. Source question carried over from the
locked source script (`claude-liam-matter-intake`, a Teardown skill-explainer
under `anthropics/claude-for-legal/`). Not a live viewer submission.

**Name usable:** N/A (no submitter).

**Source-fidelity note:** unlike some sibling `claude-for-legal` redos (e.g.
`claude-liam-hiring-review`, `claude-liam-case-brief`), this source's B00
narration carries the FULL, untruncated skill description: matter-intake
"intakes a new matter — uniform questions covering identification, conflicts,
source, risk triage, materiality, outside counsel, owners, legal hold, and
key dates; writes matter.md and history.md and appends a structured row to
_log.yaml. Use when the user says 'new matter', 'intake this matter', or
wants to bring a new matter into the portfolio." Only the LATER beats (B03,
BVDT, BHTF) carry a mid-word ~100-char truncation of that same description
("...risk triage, mat.", "...conflicts, sour.", "...conflicts, confl.") —
harmless, since B00 already supplies the complete text these beats truncate.
No fact here is missing or invented; all nine intake categories and the two
output artifacts (`matter.md`, `history.md`, `_log.yaml` row) come directly
from the source's own B00 narration_text. See BUILD-LOG.md for the full
account.
