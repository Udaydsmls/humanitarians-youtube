# QUESTION

**The question:** "Why does wrapping the same text in XML change the answer Claude gives?"

**Mode:** redo — source is
`anthropics/youtube/claude-basics/anthropic-retrieval-demo-wrapping-same-text-xml-changes/beat_sheet.json`
(scaffold only, never built: 0/8 beats filled, no SCRIPT.md, Teardown register).
This reel keeps its question, facts, and body argument, re-registers the
narration to Plain, replaces the cold open with the Brutalist Hesitant Writer,
and closes with the Humanitarians AI skin.

**Why it earns a reel:** the same product description, pasted twice — once as
plain text, once wrapped in XML tags — produces a noticeably different
summary from Claude. Nothing about the content changed. The natural
assumption is that formatting is decoration and shouldn't matter. It matters
because Claude was trained on huge amounts of consistently tagged data, which
built a strong expectation for that shape; text matching the shape it learned
gets processed more coherently.

**Naive framing (B00, corrected on screen):** "XML tags are just decoration,
right? Then why does the answer change?" → corrects "decoration" to
"a constraint."
