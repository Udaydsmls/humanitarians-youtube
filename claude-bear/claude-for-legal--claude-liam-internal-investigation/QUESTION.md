# QUESTION.md

**Question:** Claude picked up a skill called internal-investigation — does
that mean Claude itself now has judgment about who did something wrong?

**Who asked / where:** Redo-mode reel. Source question carried over from the
locked source script (`claude-liam-internal-investigation`, a Teardown
skill-explainer under `anthropics/claude-for-legal/`). Not a live viewer
submission.

**Name usable:** N/A (no submitter).

**Source-fidelity note:** the source's beat_sheet.json never received its
skill-specific "Claude's job: ___" fill — the literal placeholder `>` is
still present in three places (B00, B03, BVDT), and its "Reference:" line
("shared framework for managing internal investigations from.") is itself
cut off mid-sentence — matching the identical unfilled-`>` bug already
documented on the `claude-for-legal--claude-liam-hiring-review` and
`claude-for-legal--claude-liam-case-brief` sibling redos. The SKILL.md
internal-investigation was meant to describe
(`/Users/bear/Documents/CoWork/bear-textbooks/books/anthropics/claude-for-
legal/employment-legal/skills/internal-investigation/SKILL.md`) does not
exist on this machine (confirmed: only `youtube/` exists locally under
`anthropics/claude-for-legal/`; no `employment-legal/` directory). See
BUILD-LOG.md for how this redo handled the gap: the source's true, generic
argument (what a skill is, how it executes, why it's a specification with a
limit) is preserved and expanded to meet hai-simple's six-move spine. No
specific claim about what internal-investigation's particular employment-law
procedure actually contains is asserted anywhere — the reel treats
"internal-investigation" only as the named example of a skill-shaped folder.
