# CARRY-OUT.md

**Carry-out line (BCRY narration):**
> A Claude Code agent is one file: a description with examples that decides
> when it runs, and a body that decides what it does once it's running.

**Wrong guess it defeats:** that the thing to write is a command (B00's
hesitant-writer correction: "command" → "agent") — a command only runs when
you type it; what the builder actually wants keeps going on its own, through
several steps, which is what an agent file is for.

**Test:** if someone repeats only this sentence next week, it is still true
and still the distinction that matters — not a summary of the whole topic.
