# QUESTION.md

**Question:** Claude picked up a "skill" called matter-workspace — does that mean
it can already see everything across every matter you have, blended into one
pool, or does it keep this matter separate from the rest?

**Who asked / where:** Redo-mode reel. Source question carried over from the
locked source script (`claude-liam-matter-workspace`, a Teardown skill
explainer under `anthropics/claude-for-legal/`). Not a live viewer submission.

**Name usable:** N/A (no submitter).

**Source-fidelity note:** the source SKILL.md
(`/Users/bear/Documents/CoWork/bear-textbooks/books/anthropics/claude-for-legal/ai-governance-legal/skills/matter-workspace/SKILL.md`)
does not exist on this machine (confirmed via `find` across the whole
`anthropics/claude-for-legal/` tree — only `youtube/claude-liam-matter-workspace/`
exists locally; no `ai-governance-legal/` directory). Unlike the `matter-briefing`
sibling, this source's own beat_sheet.json narration does NOT spell out what
matter-workspace's per-matter deliverable actually is beyond generic skill
anatomy ("A SKILL.md tells Claude exactly how. Let me show you what's in it.").
The one specific, real, load-bearing fact the source DOES state, verbatim in
substance, is its B03 design tell: "The skill never reads across matters unless
Cross-matter context is on in the practice-level CLAUDE.md." That sentence is
treated as ground truth for every specific claim this redo makes about
isolation. Nothing about what matter-workspace actually organizes or produces
inside a single matter (beyond "Claude reads the Steps section and executes it
in order," also stated by the source) is invented.
