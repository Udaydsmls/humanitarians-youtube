# QUESTION — claude-for-legal--claude-liam-cold-start-interview

**Source note (redo mode, read before anything else):** `SUBJECT.json` points
`source_sheet` at
`/Users/nik/Documents/books/anthropics/claude-for-legal/youtube/claude-liam-cold-start-interview/beat_sheet.json`.
That sheet exists and is fully "built" (7 beats, all marked VIDEO/filled,
dated 2026-08-03), but its narration text carries **literal, never-filled
template placeholders** (`>`) at every point where the actual skill-specific
fact should be — `"Claude's job: >."`, `"The SKILL.md is the spec — >."`,
`"I want to >. Read the cold-start-interview skill…"` The `source_skill`
field it names,
`/Users/bear/Documents/CoWork/bear-textbooks/books/anthropics/claude-for-legal/ai-governance-legal/skills/cold-start-interview/SKILL.md`,
does not exist anywhere on this machine (searched the whole `books/` tree —
no `cold-start-interview` SKILL.md, no `ai-governance-legal` folder). Same
defect class already logged for the sibling `ai-inventory` redo in this same
family: a batch-built Teardown shell whose skill-specific fact was never
actually authored.

**The call (logged, not asked):** rather than block on a missing human
answer, this build reconstructs the evident subject from the skill's name
("cold-start-interview") into a generic, defensible account of what a
cold-start-interview practice is for and why it matters — described
generically per the fresh-script Phase 1 rule ("when in doubt, describe
behavior generically") rather than inventing specific tool names, UI, or
product claims. The one fact this reel leans on that IS a true, verifiable
Claude property (not an invented specific): a new conversation carries no
memory of prior conversations unless that context is explicitly provided —
every matter starts cold. "cold-start-interview" is read at face value as a
skill built to front-load exactly the facts a cold conversation doesn't have,
via a fixed set of questions rather than an unstructured brief. No fact here
is an invented UI element, an invented model name, or a specific product
claim.

**Question this reel actually answers:** When Claude takes on a brand-new
matter with no history, what actually gets it up to speed — a free-form
brief written from memory, or a fixed set of questions asked every time?

**Who asked, where:** nobody — reconstructed per the note above.
**Name usable:** n/a.
