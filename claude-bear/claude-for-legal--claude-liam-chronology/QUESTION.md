# QUESTION — claude-for-legal--claude-liam-chronology

**Source note (redo mode, read before anything else):** `SUBJECT.json` points
`source_sheet` at
`/Users/nik/Documents/books/anthropics/claude-for-legal/youtube/claude-liam-chronology/beat_sheet.json`.
That sheet exists and is fully "built" (7 beats, all marked VIDEO/filled,
dated 2026-07-25). **Unlike its siblings in this family**
(`claude-liam-amendment-history`, `claude-liam-aia-generation`, whose
narration carried literal unfilled `>` template placeholders), this source's
narration is genuinely written: it describes an Anthropic skill named
`chronology` — "Build or update a chronology from declared document sources
and uploads — dated events extracted, de-duped, and tagged by significance
per the matter theory. Use when the user asks to build a chronology or
timeline from a production or matter file, says 'chron from the production'
or 'what happened when', or needs a working, statement-of-facts, or
witness-specific timeline." The `source_skill` field it names,
`/Users/bear/Documents/CoWork/bear-textbooks/books/anthropics/claude-for-legal/litigation-legal/skills/chronology/SKILL.md`,
does not exist anywhere on this machine (searched the whole `books/` tree —
no `chronology` SKILL.md, no `litigation-legal` folder), so nothing beyond
what already survived into the source narration/description text can be
independently re-verified against the skill's actual instructions.

**The call (logged, not asked):** the source's facts (declared-source
extraction, de-dup, significance tagging by matter theory) are real and are
carried forward. What changes is the register and the shape: Teardown's
"Claude's job / what it gets right / what it bites" framing is replaced with
a Plain mechanism-and-limits account, illustrated with one concrete anchor
(a single event mentioned across three source documents) that the source
never had. Described generically per the fresh-script Phase 1 rule — no
invented UI, no invented product behavior beyond the two mechanics the
source's own description states.

**Question this reel actually answers:** A legal matter draws its facts from
a stack of documents that each mention dates on their own. If you need to
know what happened and what actually matters to the case, what do you build
— and how is it different from a plain list of every date in the file?

**Who asked, where:** nobody — reconstructed per the note above.
**Name usable:** n/a.
