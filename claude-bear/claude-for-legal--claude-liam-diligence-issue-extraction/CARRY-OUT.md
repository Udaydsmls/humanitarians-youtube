# CARRY-OUT.md

**Carry-out line (BCRY, read before any other narration — CARRY-OUT LAW):**

> Same documents in, same issues report out, every time. Deciding which issues actually
> kill the deal is still yours.

**Test:** repeated cold, out of context, next week — still true, still specific to this
skill (names the actual behavior: a repeatable screening pass over a checklist, scoped to
whatever the SKILL.md tells it to check), not a generic "AI can review contracts" line.
It compresses the distinction that matters (repeatable extraction vs. who judges
materiality), not the topic (due diligence in general).

**Wrong guess it defeats:** that "Claude can flag which issues kill the deal" means
Claude renders a judgment about the deal's risk — which issues actually matter, which
ones are deal-breakers. The reel's B03 states the skill's actual constraint (read each
document against the categories the SKILL.md specifies, flag anything that matches) and
stops there without judging whether that's a good or bad design; the carry-out is what a
viewer should keep: the skill's reliability is scoped to the checklist it was given, and
the actual call — which flagged issue is serious enough to hold up the deal — is still
counsel's job.

**Source of this line:** compressed from the source reel's `BVDT` verdict-recap
narration ("diligence-issue-extraction makes Claude execute one task reliably... Same
input, same output, every run. Know the limit: only what the file says.") — reused as
this video's single global carry-out. The redo's only change is which beat carries it (a
REMOTION quote card, one sentence, alone on screen) instead of a verdict-artifact card
with a "Verdict" label; a verdict belongs to Teardown, not Plain, and this one sentence
already does the job without the framing.
