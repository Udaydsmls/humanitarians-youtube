# QUESTION — claude-for-legal--claude-liam-entity-compliance

**Source note (redo mode, read before anything else):** `SUBJECT.json` points
`source_sheet` at
`/Users/nik/Documents/books/anthropics/claude-for-legal/youtube/claude-liam-entity-compliance/beat_sheet.json`.
That sheet exists and is fully "built" (7 beats, all marked VIDEO/filled,
dated 2026-07-25), but its narration text carries **literal, never-filled
template placeholders** (`>`) at every point where the actual skill-specific
fact should be — `"The skill is entity-compliance. >."`, `"Claude's job:
>."`, `"The SKILL.md is the spec — >."`, `"I want to >."` The
`source_skill` field it names,
`/Users/bear/Documents/CoWork/bear-textbooks/books/anthropics/claude-for-legal/corporate-legal/skills/entity-compliance/SKILL.md`,
does not exist anywhere on this machine (searched the whole `books/` tree —
no `entity-compliance` SKILL.md, no `corporate-legal` folder). This is the
same gap already logged for the sibling reel
`claude-for-legal--claude-liam-ai-inventory` — the source is a
teardown-format shell whose narration was never actually authored.

**The call (logged, not asked):** rather than block on a missing human
answer, this build reconstructs the evident subject from the title
("ENTITY-COMPLIANCE") and the family (`claude-for-legal`, an AI-governance
/ legal-ops skill line) into a generic, defensible account of what "entity
compliance" is in ordinary corporate legal practice — keeping every legal
entity a company has (subsidiaries, foreign qualifications) current against
its own recurring set of state filing deadlines — described generically per
the fresh-script Phase 1 rule ("when in doubt, describe behavior
generically") rather than inventing specific tool names, UI, or product
claims. No fact here is Claude-specific or unverifiable; it is the general
shape of an entity-compliance practice (annual/biennial report, registered
agent, foreign qualification, franchise tax — each on its own recurring
clock) as used in corporate legal-ops work broadly.

**Question this reel actually answers:** A company registers a subsidiary
and treats the job as finished. Is a registered entity automatically a
compliant one, or is compliance something that keeps coming due?

**Who asked, where:** nobody — reconstructed per the note above.
**Name usable:** n/a.
