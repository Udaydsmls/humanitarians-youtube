"""
Manim scenes for claude-sai-ground-truth-first. INTENTIONALLY EMPTY — this is an
all-Remotion ai-explainer reel. Every beat is either a registered Remotion
pattern rendered by remotion_scenes.py or a composed still plate built by
make_plates.py; there are no Manim beats to declare. This file exists only to
satisfy run.sh's guard, which refuses any reel without a scenes.py.

Do not add a scene declaration here, not even inside a comment or a docstring:
run.sh's gate scanner reads this file as TEXT, so the mere string would make
GATE F think a Manim beat is pending and start demanding a shot list for it.
"""
