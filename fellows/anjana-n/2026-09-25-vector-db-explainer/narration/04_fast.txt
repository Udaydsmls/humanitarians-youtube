Comparing your query against every stored vector works, but it is slow. A million vectors means a million distance calculations per query. Vector databases solve this with approximate nearest-neighbor indexes. One common approach builds a graph where each vector connects to its nearby neighbors. To search, you enter the graph at a random point, hop to the closest neighbor, and repeat. In a few hops, you reach the neighborhood of the true nearest match without scanning the full dataset.