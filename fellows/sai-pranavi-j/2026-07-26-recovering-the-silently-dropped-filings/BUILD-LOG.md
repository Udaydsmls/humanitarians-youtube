# Build log

- 2026-07-26 — Scoped video from Project 29 (Financial Regulatory Intelligence System) Layer 1 handoff notes; selected the "+73 recovered items" empty-description filter fix as the single-insight subject (over parameterized-insert fix and feed-isolation fix, both considered and set aside for future episodes).
- 2026-07-26 — Drafted 7-beat, ~90s `ai-explainer` beat sheet and narration script (Hook / Setup / Discovery / Proof / Fix / Takeaway / Sign-off).
- 2026-07-26 — Authored `beat_sheet.json`, `README.md`, `FACTCHECK.md`, `SOURCES.md`, `BUILD-PROMPT.md`, `PEDAGOGY.md`, `SHOTLIST.md`, `PROMPTS.md`.
- 2026-07-26 — Voice confirmed: `af_kore` doesn't exist in the installed `brutalist` toolkit (only `am_onyx`/`af_bella`). Fellow confirmed **Bella (`af_bella`)**, matching the `hai` persona and `@HumanitariansAI` channel.
- 2026-07-26 — Fact-check resolved: B00 "for months" removed (unmeasured dramatization); B04 "production database" phrasing flagged, fellow elected to keep it.
- 2026-07-26 — B01 narration expanded to spell out each regulator's full name (SEC/FINRA/CFTC) and to distinguish the two Federal Register feeds (confirmed against `workflow.dev.json`: one is a general securities/investment keyword feed, the other filtered to the CFTC agency) — diagram labels updated to `FedReg (Sec.)` / `FedReg (CFTC)` to match.
- 2026-07-26 — Audio locked (Kokoro `af_bella`, all 7 beats).
- 2026-07-26 — Layer 1 previz built and iterated in this fellows folder only (toolkit at `brutalist/` used read-only as the render engine, never edited): B01 (pipeline diagram) and B04 (before/after counter) as Manim scenes from the start. B00, B02, B03, B05, B06 were originally planned as vox-still/Remotion/card beats; the two named Remotion patterns (`ClaudeCodeDiffView`, `HumanitariansResearchReport`) don't exist in the installed toolkit, and building them would mean adding components to the shared `brutalist/` repo, which was out of scope — so all 5 were rebuilt as self-contained Manim scenes in this reel's own `scenes.py` instead.
- 2026-07-26 — Fixed a toolkit slate-label truncation bug (hard 80-char single-line cut, no word-wrap) without touching the toolkit: added a short `new_visual_element` field per slate beat, which `compile.py` prefers over the full `narration_text`.
- 2026-07-26 — GATE V frame QC: the review cut's own timecode watermark (top-right, review-only) was flagged as a false-positive "edge-bleed" on every beat; confirmed against the true clean master (no watermark), which came back at 0 BLOCKER defects after fixing one genuine issue (B01's rightmost stage box sat just past the title-safe margin — nudged in). Remaining: 11 MAJOR "underfill" (several new Manim cards are sparse relative to the frame) and a mild contrast note on B01 — cosmetic polish, not correctness.
- 2026-07-27 — Fellow watched the rendered master (`2026-07-26-recovering-the-silently-dropped-filings.mp4`, 88.5s, 7/7 beats real, no slates) and confirmed it looks correct.
- GATES CLOSED — plan, fact-check, narration, audio lock, previz. See `beat_sheet.json` → `metadata.gates` for the dated record.
- OPEN — Cosmetic polish pass (underfill/contrast), optional, not blocking.
- NOT AUTHORIZED — Publishing.

## 2026-08-26 — Program-feedback rebuild: 4K + 2 new front beats (v2)

- 2026-08-26 — Program feedback received: (1) resolution must be 4K (3840x2160), was 1920x1080; (2) rebuild on the just-updated `brutalist/` toolkit and fix any incompatibilities; (3) add two new beats at the very front — a silent title card and a spoken executive-summary/personal-intro card — following the pattern proven in this fellow's sibling reel `fellows/sai-pranavi-j/2026-08-17-why-ai-generated-code-still-needs-a-human`'s v3 rebuild (its `B00_TitleCard` + renumbering).
- 2026-08-26 — Read the toolkit's current state before touching anything (per instruction, not assumed from memory): `RENDER-4K-AND-UPLOAD.md` (new doc — `./art run`/`./art final` workflow, 4K-native by default), `runtime/scripts/run.sh` (now hardcodes `RES="3840,2160"` for every Manim render regardless of the `--height` flag passed to `compile.py`, and defaults `HEIGHT=2160`), `runtime/scripts/generate_audio_kokoro.py`, `runtime/scripts/compile.py` (confirmed `build_master_audio()`'s `all(p.exists())` per-beat-audio contract — the exact mechanic the sibling reel's BUILD-LOG had already traced), `runtime/qc/static_scene_check.py`, `runtime/qc/gate_shape.py` (new — finance-only, no-op for this non-finance reel), `runtime/qc/wcag_margin_check.py`, `runtime/qc/final_frame_check.py`.
- 2026-08-26 — Useful discovery: this reel's existing `manim/B00.mp4`...`manim/B06.mp4` (from the 2026-07-26 build) were already native 3840x2160 — `manim -qk -r 3840,2160` was already the toolkit's Manim render resolution even when the old build's final `compile.py --height` call downscaled the *master* to 1080p. That meant the 4K requirement was satisfiable by recompiling at `--height 2160` without re-rendering the 7 unchanged Manim scenes at all — only the 2 new beats needed an actual `manim` render pass.
- 2026-08-26 — Renumbered all 7 existing beats +2 (B00→B02 HOOK, B01→B03 SETUP, B02→B04 DISCOVERY, B03→B05 PROOF, B04→B06 FIX, B05→B07 TAKEAWAY, B06→B08 SIGN-OFF) in `beat_sheet.json` (beat_id, `shot.manim.scene`, `audio_file`, `build.src`) and `scenes.py` (class names), and renamed the on-disk assets to match (`manim/B0N.mp4`, `mp3/beat-B0N.mp3`, moved highest-ID-first to avoid overwrite collisions during the shift). Cleared stale `clips/`, `media/`, `mp4/`, `images/`, `layout_audit*`, `_qc/`, `qc-sheet.png`, and `mp3/timings.json` — all compile.py-regenerated artifacts, not source — rather than leave them keyed to the old beat IDs.
- 2026-08-26 — Added `B00_TitleCard` (silent — title pulled verbatim from `metadata.title`, "The Pipeline That Was Lying to Me", + `@HumanitariansAI`) and `B01_ExecSummary` (spoken personal-intro card: fellow's name + role + one-line summary) to `scenes.py`, plus a `fit()` helper (ported from the sibling reel's `scenes.py`, which this file didn't have yet). Added corresponding beat objects to `beat_sheet.json`. Confirmed via the same static-analysis-then-trace method the sibling reel used: `compile.py`'s `build_master_audio()` requires every beat's `audio_file` to exist or the whole film's audio falls back to silence — so B00 gets a REAL silent mp3 (`ffmpeg -f lavfi -i anullsrc=r=44100:cl=stereo -t 4.2`, measured 4.23s), never `audio_file: null`.
- 2026-08-26 — Toolkit-compatibility check (per program feedback item 2): ran the updated `static_scene_check.py` and `wcag_margin_check.py` against all 7 unchanged, renamed scenes (B02-B08) — 0 errors, only the same pre-existing cosmetic warnings (text-only/no-shapes-recorded) already known from the original build. `gate_shape.py` (new) is finance-reel-only and no-ops cleanly on this non-finance reel. **No incompatibilities found or fixed on the 7 existing scenes** — the toolkit update didn't break anything here.
- 2026-08-26 — GATE A caught a real static error on the new `B01_ExecSummary`: a lone static `accent` `Line` with no second non-text shape for the rest of the hold tripped "shapes never change — 1 distinct shape-state across 3 frames" (same class of defect the sibling reel's `B00_TitleCard` hit and fixed the same way). Fixed by adding a `summary_underline` `Line` created after the `accent` line — mirroring this same file's own pre-existing `B08_BrandOutro` (`tagline_underline`) pattern — giving 2 distinct shape-states. GATE A/W re-run clean on both new scenes after the fix.
- 2026-08-26 — Generated B00's silent mp3 (ffmpeg anullsrc, 4.23s measured) and ran `generate_audio_kokoro.py` on the whole folder for the 8 narrated beats: new B01 measured 13.87s; **all 7 renamed-but-unchanged beats came back at exactly their original 2026-07-26 measured durations** (B02 9.70s, B03 26.81s, B04 18.79s, B05 8.23s, B06 11.74s, B07 8.18s, B08 5.04s) — confirms Kokoro is deterministic for unchanged text/voice and that the renumbering didn't silently touch any narration.
- 2026-08-26 — First `run.sh` pass (rendered only the 2 pending scenes, B02-B08 skipped as already-filled): GATE A/W clean, GATE B (`manim_layout_audit.py`) clean on both new scenes, 9/9 beats compiled, 106.6s total, master + review cut both written. GATE V (which checks the review `-slate.mp4` by default) flagged the review cut's own timecode-watermark edge-bleed on all 9 beats — the same confirmed false-positive documented in this fellow's other reels' prior builds (`-slate.mp4` review label sits in the title-safe margin by design). Checked the TRUE clean master directly (`final_frame_check.py --mp4 <slug>.mp4`, not the slate): 0 BLOCKER, but **2 new MAJOR `underfill` defects on B00 (50% coverage) and B01 (23% coverage)** — genuinely new, introduced by the 2 new beats, not carried over from the old build.
- 2026-08-26 — Fixed both underfill defects by measuring offline against **real Manim text metrics** (imported the actual `manim` package directly, built the same `Text`/`Line` mobjects with the same params, read `.width`/`.height` — not the render-free `static_scene_check.py` stub, and not guesswork) rather than iterating blind: B00 at `buff=1.0` measured width=11.40/height=4.04 → cover=0.50; raising to `buff=1.3` (width unchanged, height→4.94) → cover=0.61. B01 at font_size 46/22/27 + `buff=0.45` measured cover=0.22; raising to font_size 58/28/34 + `buff=1.3` → width=10.51, height=5.33, cover=0.61, with 1.15/0.94 units of clearance from the safe edge on width/height (no edge-bleed risk introduced). Verified both fixes render-accurate by re-rendering the two scenes standalone at full 3840x2160 and running `final_frame_check.py --frames-dir` directly on real extracted frames (not the offline estimate) before touching the reel's own asset folders: **0 BLOCKER, 0 MAJOR** on both.
- 2026-08-26 — Cleared the pre-fix `manim/B00.mp4`/`B01.mp4` and `clips/B00.mp4`/`B01.mp4` (same stale-slot risk the sibling reel's BUILD-LOG documented) and re-ran `run.sh` end to end: GATE A/W/B clean, 9/9 beats compiled real (no slates), master + review cut rewritten at 3840x2160, 106.6s.
- 2026-08-26 — Ran `./art final <reel>` per `RENDER-4K-AND-UPLOAD.md`'s documented workflow. **Toolkit doc/code discrepancy found and noted, not a blocker:** the doc describes `./art final` as running "GATE T" against a `TYPECHECK.md` and producing `<slug>-cut.mp4`; tracing the actual code (`art`'s `final` case → `compile.py --height 2160`, no `--review`) shows no GATE T/TYPECHECK.md logic exists anywhere in `compile.py` or `run.sh`, and the output path is `<slug>.mp4` (overwriting the master in place), not `<slug>-cut.mp4`. Followed the actual code behavior (which is what matters for the deliverable) rather than the doc's description; this reel's paperwork doesn't carry a `TYPECHECK.md` and none was needed. Output: `2026-07-26-recovering-the-silently-dropped-filings.mp4`, confirmed via `ffprobe` at **3840x2160, 24fps, h264, 106.458s**.
- 2026-08-26 — **GATE V on the true clean 4K master** (`final_frame_check.py --mp4 <slug>.mp4`, not the watermarked `-slate.mp4` — same known false-positive re-confirmed: the slate cut still shows 18 BLOCKER from its own review-label burn-in, the true master shows 0): **0 BLOCKER, 11 MAJOR**. Cross-checked category-by-category against the original 2026-07-26 build's accepted list ("11 MAJOR underfill... and a mild contrast note on B01"): identical count and category, just under the shifted beat IDs — underfill on B02/B05/B06/B07/B08 (was B00/B03/B04/B05/B06) and low-contrast on B03 (was B01, the pipeline-diagram beat). **No new defects survive in the renumbered body beats** — the only new defects this rebuild introduced (B00/B01 underfill) were caught and fixed above, before this final check.
- 2026-08-26 — Spot-checked evidence legibility directly against real extracted frames (not asserted) for the self-assessment below: B04's removed-filter code (legible, boxed, tagged REMOVED), B05's four named recovered filings (legible serif type), and B06's before/after count. One false alarm caught and corrected in the process: a frame grabbed at t=87.5s (an imprecise `ffmpeg -ss <time> -i` seek before the input) showed "366" instead of 370 mid-count-up-adjacent; re-extracting frame-accurately at t=89.0s (well past the count-up's own 2.7s settle point within the beat) confirmed the correct, settled "370" — matching `FACTCHECK.md`'s and the narration's claim. Not a defect; documented so the same seek pitfall isn't repeated.
- 2026-08-26 — Copied `PROOF.md` (generic protocol doc, not video-specific) from `fellows/sai-pranavi-j/2026-07-27-how-facial-recognition-actually-works/PROOF.md` into this folder and wrote `SELF-ASSESSMENT.md`: an honest scoring against PROOF's actual rubric (4/12 — strong on Worked Example, weak/absent on Explicit Framework, Falsifiability, and Active Task) and Production Gate (FAIL — no claim in this video carries a visible source/citation on screen, even though the artifacts themselves — filing names, the measured count — are legible; side-by-side comparison and evidence-legible-at-assertion both otherwise PASS). Noted explicitly that this is a genre mismatch (PROOF is built for skeptical/framework-teaching explainers; this is a single-insight `ai-explainer` case study that already passes its own `PEDAGOGY.md` act-structure rubric) rather than treating the low score as a defect to chase.
- 2026-08-26 — Updated `beat_sheet.json` metadata/gates (plan reopened+closed for the rebuild, audio_lock/shopping_list/previz extended to 9 beats, narration gate noting the fellow's re-review of this specific cut is not yet recorded), `README.md` production state, and `PEDAGOGY.md` (pointer section to `SELF-ASSESSMENT.md`). `SHOTLIST.md` updated with the 2 new beats and the renumbered table.
- GATES (v2, 2026-08-26) — plan reopened+closed for the rebuild scope, fact-check unchanged (no claims changed — see `FACTCHECK.md`, still valid), audio lock extended to 9 beats (7 confirmed unchanged + 2 new), previz COMPLETE (9/9 real, 4K, GATE A/W/B/V all clean or carrying only the pre-existing accepted MAJOR list). **Narration gate for this specific 9-beat cut is NOT yet fellow-reviewed** — flagging honestly rather than marking approved by inheritance from the 2026-07-27 review of the old 7-beat cut.
- NOT AUTHORIZED — Publishing. (Per this task's explicit instruction: no push to `brutalist/`, no upload — reporting back for a separate publishing pass.)

## 2026-08-28 — 9:16 Shorts derivative built (`short/`)

- 2026-08-28 — Ran `runtime/scripts/shorts.py` against this reel (`--keep B00..B08` to be explicit) per THE SHORTS LAW: at 106.46s this reel is **under** the 180s cap, so the whole reel reformats 16:9→9:16 as-is — **0 beats dropped, no outro rewrite, every beat's mp3 reused unchanged** (symlinked from the parent's `mp3/`). Scaffolded `short/` with `beat_sheet.json` (`aspect_ratio: "9:16"`), a silent branded endcard (media/END.png + mp3/beat-END.mp3, 4.5s), and correctly flagged all 9 beats as GENERATED (Manim) — "no cut; needs a portrait scene in short/scenes.py" — since THE REFORMAT RULE never auto-crops generated graphics.
- 2026-08-28 — Authored `short/scenes.py`: 9 portrait (1080x1920) Manim scenes, one per beat, same class names/PALETTE/MONO as the parent's `scenes.py`, same per-beat animation timing (every `self.play`/`self.wait` value copied beat-for-beat, since the audio is identical) — only the geometry was redesigned for a 3.6-4.5-unit-wide safe column instead of the parent's ~12-14-unit-wide frame. The two beats named in the build brief as needing real redesign, not a mechanical shrink:
  - **B03 (pipeline diagram):** parent has 5 feeds stacked LEFT, 4 stages in a row RIGHT (side-by-side). Portrait restacks it top-to-bottom: 5 feed chips in one horizontal row up top, converging through a merge point down into a vertical column of the 4 stages below — reads the same order the narration walks through (feeds → pipeline) but top-to-bottom instead of left-to-right. The empty-description filter callout moved from below-the-first-stage (no vertical room in the old plan) to beside it, in the little width the narrow column still has to spare.
  - **B06 (before/after count):** parent has 297/370 side by side at font_size 80. Restacked BEFORE above, AFTER below (matching the narration's own order — "the result: seventy-three additional items"), both counters enlarged (final font_size 168) to also help meet GATE V's canvas-fill floor. Used a build-then-measure pattern (a throwaway `VGroup.arrange()` for correct spacing, `.get_center()` read off the AFTER slot, then the `always_redraw` ValueTracker counter swapped in at that resolved position) rather than hand-picked coordinates.
- 2026-08-28 — **Found and fixed a Manim CLI quirk that would have silently broken every portrait render in this toolkit**, not just this reel's: `manim -qk --fps 24 -r "1080,1920" scenes.py <Scene>` sets `pixel_width`/`pixel_height` from `-r` but does **not** recompute `frame_width` to match — it keeps the 16:9 default (14.22) and stretches `frame_height` instead, so a scene composed against an assumed 4.5-unit-wide frame actually renders at roughly a third of its intended size, clustered in the middle of a much taller effective canvas. Confirmed via a probe scene: a point placed at manim-unit `(0, 4, 0)` (the intended frame top) landed only ~1/3 of the way to the real edge. Traced this to a **pre-existing, already-applied fix** in the toolkit's own shared `runtime/manim/animated_graphics.py` (a "Portrait sync (the bn_layout fix)" comment block, lines 73-83) — the shared fixture already works around this, but a reel's own standalone `scenes.py` (like this one) doesn't inherit it. Ported the identical fix into `short/scenes.py` (recompute `config.frame_width = config.frame_height * pixel_width/pixel_height` at import time). Did **not** touch `brutalist/` — the fix lives entirely in this reel's own `short/scenes.py`.
- 2026-08-28 — GATE A (`static_scene_check.py`) + GATE W (`wcag_margin_check.py --palette humanitarians`) run on every one of the 9 new scenes before any render: all 9 clean (the 3 `GATE A` "no shapes recorded — text-only" warnings on B05/B06/B07 are pre-existing and identical in the parent's own 16:9 scenes — confirmed by running the same check against `../scenes.py`, not assumed).
- 2026-08-28 — Rendered all 9 scenes at native 1080x1920 (`manim -qk --fps 24 -r "1080,1920"`), ran GATE B (`manim_layout_audit.py --portrait --curve-strict`) post-render on each — iterated three real layout bugs the portrait redesign introduced, each confirmed by GATE B before being called fixed: B03's title (pushed down when its `to_edge` buff grew) collided with the feed-chip row until the row was repositioned `next_to(title, DOWN, ...)` instead of a fixed y; B03's filter-note callout twice landed outside the safe x-margin until its width/buff were tightened; B06's caveat line twice landed outside the safe y-margin as its numbers grew, fixed by tightening the layout's internal buffs.
- 2026-08-28 — Compiled via `compile.py short --review --height 1920` then `compile.py short --height 1920` (no `--review`) per the aspect-driven width math (`h * 9/16`, rounded to even): **1080x1920**. Ran GATE V (`final_frame_check.py short --mp4 short/<slug>-short.mp4`) on the TRUE clean master, not the watermarked `-slate.mp4` — same documented false-positive re-confirmed here (the slate cut read 20 BLOCKER "edge-bleed" from its own review-label burn-in on every beat; the true master read 0 BLOCKER from the first pass).
- 2026-08-28 — First true-master GATE V pass: 0 BLOCKER, 16 MAJOR (`underfill` on B00/B01/B03/B04/B06/END, `low-contrast` on B03) — genuinely low coverage (2-7%), not a false positive; confirmed by extracting real frames (not asserted) showing every card's content clustered in a compact band with a huge empty top/bottom margin. Root-caused to the same CLI quirk above (before the fix, "portrait" scenes were rendering at ~1/3 scale in the middle of an oversized canvas) — fixing the frame-width bug alone dropped the MAJOR count from 16 to 9 the moment cards actually rendered at their intended geometry.
- 2026-08-28 — Root-caused the remaining `low-contrast` on B03 (separation 0.20-0.21, both GATE V sample points) to two distinct real causes, each fixed: (1) the filter-note's `Write()` animation traces a progressive stroke outline that briefly renders as a pale near-white sliver before the crimson fill catches up, and GATE V's own 50%/85% sample points happened to land inside that transient window on both samples — swapped to `FadeIn()` (ramps the same crimson fill's opacity, no outline trace) and the transient disappeared; (2) independently, the stage/feed `RoundedRectangle` fill colors (opacity 0.12/0.15) were blending to just barely over GATE V's ink-detection threshold (measured: teal fill at 0.15 opacity → max channel diff 31.8, threshold is 28), so their large pale interiors were being counted as "content ink" and dragging the whole frame's average contrast down toward the background — lowered both fill opacities (0.12→0.05, 0.15→0.06) to drop them back under the threshold, a barely-visible cosmetic change that doesn't alter the diagram's read.
- 2026-08-28 — Closed the remaining genuine `underfill` MAJORs (B00, B04, B06, B07, B08) with real Manim-measured buff/font increases (same "measure real `.width`/`.height`, don't guess" method the 2026-08-26 4K rebuild used for B00/B01), re-running GATE A/W/B after every change and before every re-render: B00 buff 0.75→1.1; B04's code-block buff 0.45→0.6; B06's counters enlarged in steps (font_size 76→92→104→118→150→168, converging on cover ≈0.50 before the final font pass, tightening buffs each round to stay inside the safe y-margin as the numbers grew) with its title/layout gap retuned twice to clear GATE B's margin check; B07 rewritten as bigger poster-style type (font_size 30/32 → 58/62, both lines wrapped to two lines) rather than only widening the gap between two short lines, which real measurement showed would need an ~4.8-unit buff alone to hit the floor — an absurd, obviously-wrong-looking gap for 2 short lines, so type size carried the fix instead of spacing; B08 similarly (handle/tagline font_size 30/18 → 66/42, buff 0.35→1.95) once real measurement showed both lines were already width-capped by the safe column, so only a bigger buff (not bigger font) could move its coverage.
- 2026-08-28 — Final true-master GATE V result: **0 BLOCKER, 0 MAJOR on all 9 authored Manim beats (B00-B08)**. The one remaining MAJOR (`underfill`, 30-42% coverage) is on the auto-generated silent `END` card — a PIL-drawn branded card produced by `shorts.py`'s own `endcard_png()` function (fixed handle font_size, fixed vertical layout), not one of this reel's 9 Manim scenes and not editable without touching `brutalist/` (out of scope, read-only per this task's instructions). Used the script's own sanctioned `--next` override to give the card substantive tease copy (raised its coverage 2%→42%) but could not close the remaining gap without either a hand-authored `pantry/END-916.png` replacement or a toolkit change to `endcard_png()`'s hardcoded layout — **flagging this as a human design call**, not silently accepting or silently forcing a fix outside scope.
- 2026-08-28 — Verified the final master via `ffprobe`: **1080x1920, h264/aac, 24fps, 110.96s** (matches the beat sheet's own `total_estimated_duration_seconds: 111.09`, rounding). `beat_sheet.json`'s build stamp confirms **10/10 slots filled, `slates: []`** — 9 real MANIM beats (B00-B08) + 1 real STILL (END), no placeholders. Cleaned up render scratch (`media/videos/`, `__pycache__`, debug `layout_audit_frames/`) from `short/` before calling this done.
- Short master: `short/2026-07-26-recovering-the-silently-dropped-filings-short.mp4` — 1080x1920, 110.96s, 10/10 real beats, 0 BLOCKER / 0 MAJOR on all 9 authored scenes (1 MAJOR remaining on the non-Manim endcard, flagged above).
- NOT AUTHORIZED — Publishing. (Same instruction as the long: reporting back, no push to `brutalist/`, no playlist assignment yet.)
