"""
Manim scenes for claude-liam-ai1-four-gates ("Claude, Constitutional.")

No Manim beats in this reel — this machine has no pangocairo/pkg-config, so
Manim can't install (see BUILD-LOG.md). Every beat is Remotion (ClaudeComposerAsk,
FluencySegmentCard, DivergentFates, ClaudeScienceChipGrid, ClaudeScienceLayerStack,
BinaryBranch, ClaudeVerdictArtifact, ClaudeTitleOutro) or a VOX still (real
archival photographs in media/, animated by compile.py's Ken Burns). This file
exists only to satisfy run.sh's per-reel scenes.py guard (it refuses to render
the shared animated_graphics.py fixture scenes into a reel that isn't
vox-electoral-college).
"""
