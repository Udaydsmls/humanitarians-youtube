# SHOTLIST — death-of-the-generic-resume/short (9:16 Shorts cut)
# Derived from the parent 16:9 reel via shorts.py. Parent is 4:00.5, over
# the 3:00 Shorts cap, so shorts.py auto-dropped 4 beats (longest middle
# beats first, hook/hero/outro protected): B07, B08, B09, B11 — the whole
# "AI arms race" act (tailored-resume fan-out, identical-grid, filtered-out)
# and the closing "Your AI <-> negotiating <-> Their AI" beat. The rewritten
# outro (B14) names what got cut and points viewers to the long.

## Composer/outro beats — rewired to portrait (916) compositions

B00 · ClaudeComposerAsk916 (Remotion, portrait re-render) — plain ask hook
B13 · ClaudeComposerAsk916 (Remotion, portrait re-render) — HANDOFF LAW
B14 · ClaudeTitleOutro916 (Remotion, portrait re-render) — outro NARRATION
      rewritten (only audio regenerated for this Short) to name the cut
      arc and send viewers to the long; title/handle unchanged

## Manim GRAPHIC beats — portrait-relaid-out in short/scenes.py

B00B · B00B_AgrimaIntro — presenter card ("Hi, I'm Agrima." + lead-in),
       single-column portrait layout
B01   · B01_TheOutreach — recruiter reaches out -> you apply, stacked
        vertically (was side-by-side in the parent) with a downward arrow
B02   · B02_InstantReject — Applied -> clock -> Rejected, stacked
        vertically, footer line below
B03   · B03_Gaslit — the phrase, named (vertical stack, unchanged content)
B04   · B04_QuoteDeny — "no AI auto-rejects candidates" quote card,
        narrower/taller portrait card
B05   · B05_QuoteAdmit — "we pre-screen for hard requirements" quote card
        + checklist, narrower/taller portrait card
B06   · B06_BlackBox — Application -> [ ? ] -> Rejected, stacked
        vertically (was a horizontal row in the parent)
B10   · B10_NotDying — minimal reframe card (vertical stack, unchanged)
B12   · B12_AboutYou — three-line closing typographic beat (unchanged)

## Dropped from this Short (content lives only in the parent long)

B07 · B07_TailoredStack — one resume -> many AI-tailored versions
B08 · B08_IdenticalGrid — grid of near-identical resume cards
B09 · B09_FilteredOut — same grid, one card flagged "FILTERED"
B11 · B11_TwoAIs — "Your AI <-> negotiating <-> Their AI, you: waiting" —
      the closing metaphor's visual anchor; the rewritten B14 outro
      explicitly teases this ("it's really not about resumes dying at
      all... watch the whole story") to send viewers to the long instead
      of resolving the metaphor here

## Endcard

END · silent branded card, ~4.5s, generated by shorts.py (endcard_png);
      handle corrected to @HumanitariansAI (shorts.py's default
      @nikbearbrown placeholder is not what this reel uses); Next: line
      set via --next to tease the AI-arms-race arc

## Notes

- No pantry / archival stills used — every visual is either a Claude-skin
  Remotion portrait (916) beat or a from-scratch portrait Manim scene
  (short/scenes.py). No open pantry slots.
- Portrait Manim scenes require `config.frame_width = 4.5` /
  `config.frame_height = 8.0` set explicitly at the top of scenes.py, and
  all text/cards are guarded to the 1.95-half-width portrait safe band
  (manim_layout_audit.py --portrait: safe_w=1.95, safe_h=3.4).
- Only B14's narration was regenerated (Kokoro) — every other beat reuses
  the parent's mp3 unchanged, per THE SHORTS LAW.
- Measured total: ~169.4s (2:49.4), comfortably under the 3:00 cap.
- @HumanitariansAI is used throughout (composer folderLabel, outro handle,
  and the corrected endcard handle) — matches branding precedent set on
  the parent reel and this user's other reels.
