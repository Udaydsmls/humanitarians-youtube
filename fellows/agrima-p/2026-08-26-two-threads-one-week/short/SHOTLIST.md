# SHOTLIST — two-threads-one-week/short (9:16 Shorts cut)
# Derived from the parent 16:9 reel via shorts.py. Parent is only 2:05.1,
# already under the 3:00 Shorts cap, so NO beats were dropped — all 11
# beats + the silent endcard carry over unchanged in content, just
# reformatted to portrait.

## Composer/code/outro beats — rewired to portrait (916) compositions

B00 · ClaudeComposerAsk916 (Remotion, portrait re-render)
B02 · ClaudeComposerAsk916 (Remotion, portrait re-render)
B03 · ClaudeCodeBeat916 (Remotion, portrait re-render)
B05 · ClaudeComposerAsk916 (Remotion, portrait re-render)
B06 · ClaudeCodeBeat916 (Remotion, portrait re-render)
B09 · ClaudeComposerAsk916 (Remotion, portrait re-render)
B10 · ClaudeTitleOutro916 (Remotion, portrait re-render — same title/handle
      as the parent, no rewrite needed since nothing was cut)

## Manim GRAPHIC beats — portrait-relaid-out in short/scenes.py

B01 · B01_AgrimaIntro — presenter card ("Hi, I'm Agrima.") + week summary,
      single-column portrait layout
B04 · B04_LogV1 — the two log threads (WRITING, LOON PROJECT) stacked in
      ONE column instead of side-by-side (portrait is too narrow for two
      columns) — every real item + status, no scoring
B07 · B07_LogV2 — SAME stacked layout, now with a "* highlight:" line per
      thread block from the real weekly_log_v2.py run
B08 · B08_Summary — WRITING / LOON PROJECT recap cards stacked vertically
      instead of side-by-side, same closing tagline as the parent

## Endcard

END · silent branded card, ~4.5s, generated by shorts.py (endcard_png);
      handle corrected to @HumanitariansAI (shorts.py's default
      @nikbearbrown placeholder was not what this reel uses)

## Notes

- No pantry / archival stills used in this reel — every visual is either a
  Claude-skin Remotion portrait (916) beat or a from-scratch portrait Manim
  scene (short/scenes.py). No open pantry slots.
- Portrait Manim scenes require `config.frame_width = 4.5` /
  `config.frame_height = 8.0` set explicitly at the top of scenes.py
  (Manim doesn't derive it from -r).
- No narration was rewritten and no outro funnel line was added, since no
  beats were cut from the parent — this Short IS effectively the full reel,
  just in portrait.
- @HumanitariansAI is used throughout (composer folderLabel, outro handle,
  and the corrected endcard handle) — matches the branding precedent set on
  the parent reel and the sibling ai-support-shift reel.
