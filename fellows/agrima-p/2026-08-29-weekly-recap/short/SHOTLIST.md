# SHOTLIST — weekly-recap/short (9:16 Shorts cut)
# Derived from the parent 16:9 reel via shorts.py. Parent is only 2:01.2,
# already under the 3:00 Shorts cap, so NO beats were dropped — all 11
# beats + the silent endcard carry over, just reformatted to portrait.

## Composer/code/outro beats — rewired to portrait (916) compositions

B00 · ClaudeComposerAsk916 (Remotion, portrait re-render)
B02 · ClaudeComposerAsk916 (Remotion, portrait re-render)
B03 · ClaudeCodeBeat916 (Remotion, portrait re-render)
B05 · ClaudeComposerAsk916 (Remotion, portrait re-render)
B06 · ClaudeCodeBeat916 (Remotion, portrait re-render)
B09 · ClaudeComposerAsk916 (Remotion, portrait re-render)
B10 · ClaudeTitleOutro916 (Remotion, portrait re-render — same title/handle
      as the parent, no rewrite needed since nothing was cut)

## Manim GRAPHIC beats — portrait-relaid-out in short/scenes.py

B01 · B01_NotAHighlightReel — typographic reveal, single-column portrait
B04 · B04_FlatWeek — three cards stacked in ONE column instead of a row
      (portrait is too narrow for a 3-wide row)
B07 · B07_SplitWeek — DONE/STARTING NEXT groups stacked vertically instead
      of side-by-side columns
B08 · B08_TheLesson — three-line typographic beat, unchanged content

## Endcard

END · silent branded card, ~4.5s, generated by shorts.py (endcard_png);
      handle set to @HumanitariansAI via --handle at plan time

## Notes

- No pantry / archival stills used — every visual is either a Claude-skin
  Remotion portrait (916) beat or a from-scratch portrait Manim scene
  (short/scenes.py). No open pantry slots.
- Portrait Manim scenes require `config.frame_width = 4.5` /
  `config.frame_height = 8.0` set explicitly at the top of scenes.py, with
  all text/cards guarded to the 1.95-half-width portrait safe band
  (manim_layout_audit.py --portrait: safe_w=1.95, safe_h=3.4).
- No narration was rewritten — every beat reuses the parent's mp3 unchanged,
  since nothing was cut (THE SHORTS LAW point 1: at/under the cap, the
  whole reel reformats as-is).
- Endcard PNG must be regenerated at native 2160x3840 (not the shorts.py
  default 1080x1920) if compiling at true 4K — same fix applied on the
  death-of-the-generic-resume/short build (Windows has no Mac/Linux serif
  path, so shorts.py's find_serif() silently falls back to PIL's tiny
  bitmap font; use C:\Windows\Fonts\georgia.ttf directly).
- @HumanitariansAI used throughout, matching branding precedent.
