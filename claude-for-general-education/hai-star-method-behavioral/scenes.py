# scenes.py — hai-star-method-behavioral
#
# This reel has no Manim beats: every beat is a Remotion pattern
# (ClaudeComposerAsk, StateCardPair, LabelChip, QuoteCard, ClaudeVerdictArtifact,
# ClaudeTitleOutro), rendered by runtime/scripts/remotion_scenes.py, not Manim.
# This stub exists only so run.sh's per-reel-scenes guard finds a file (it
# refuses to fall back to the shared animated_graphics.py fixture scenes,
# which belong to a different reel). No Scene classes are defined here on
# purpose — there is nothing for `manim` to render for this video.
